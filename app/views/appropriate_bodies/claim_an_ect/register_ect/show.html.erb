<% page_data(
  title: "#{@pending_induction_submission.first_name} #{@pending_induction_submission.last_name} is registered",
  header: false,
  error: @pending_induction_submission.errors.present?)
%>


<%=
  govuk_panel(title_text: "Success", text: "#{@pending_induction_submission.first_name} #{@pending_induction_submission.last_name} has been registered with #{@appropriate_body&.name}")
%>

<div class="govuk-button-group">
  <%= govuk_button_link_to("Register another ECT", "#", secondary: true) %>
  <%= govuk_button_link_to("Return to the homepage", "#", secondary: true) %>
</div>
